syntax = "proto2";


// couchbase key: group_id:topic_id:category_id
// couchbase data:
message Item2ItemData
{
    // it's doc hashes (bahroma keys)
    repeated uint64 docs    = 1 [ packed = true ];
}
