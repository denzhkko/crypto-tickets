syntax = "proto2";

package wiki_page.pb;

message WikiPage {
    // It is page title
    // Need for kv_daemon
    optional string query_text = 1;
    optional string page_content = 2;
    optional string error_msg = 3;
}
